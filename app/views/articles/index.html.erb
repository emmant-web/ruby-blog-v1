<div class="max-w-4xl mx-auto px-4 py-8">
  <h1 class="text-4xl font-bold text-gray-800 mb-2">Articles</h1>
  <p class="text-gray-600 mb-6">
    Our blog has 
    <span class="inline-block bg-blue-100 text-blue-800 text-sm font-medium px-2.5 py-0.5 rounded">
      <%= Article.public_count %>
    </span> 
    articles and counting!
  </p>

  <div class="space-y-4 mb-8">
    <% @articles.each do |article| %>
      <% unless article.archived? %>
        <div class="bg-white shadow-md rounded-lg p-4 hover:shadow-lg transition">
          <%= link_to article.title, article, class: "text-xl font-semibold text-blue-600 hover:underline" %>
        </div>
      <% end %>
    <% end %>
  </div>

  <%= link_to "➕ New Article", new_article_path, class: "inline-block bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition" %>
</div>
